# 🎯 AlgoCampus - Visual System Overview

## System Architecture (One Page)

```
┌────────────────────────────────────────────────────────────────────────┐
│                          FRONTEND LAYER                                 │
│  React + TypeScript + TailwindCSS + Algorand Wallet Connect            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐              │
│  │  Auth    │  │  Groups  │  │ Expenses │  │Settlements│              │
│  │  View    │  │  View    │  │  View    │  │   View    │              │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘              │
└────────┬──────────────────────────────────────────────────┬────────────┘
         │ REST API (JSON)                     WebSocket    │
         │                                                   │
┌────────▼───────────────────────────────────────────────────▼───────────┐
│                        BACKEND API LAYER                                │
│                    FastAPI + Python 3.11                                │
│  ┌─────────────────────┐  ┌──────────────────┐  ┌─────────────────┐  │
│  │  Authentication      │  │  Business Logic  │  │  Indexer Service│  │
│  │  - Challenge/Verify  │  │  - Groups        │  │  - Block poll   │  │
│  │  - JWT tokens        │  │  - Expenses      │  │  - Event parse  │  │
│  │  - Signature check   │  │  - Settlements   │  │  - DB sync      │  │
│  └─────────────────────┘  └──────────────────┘  └─────────────────┘  │
│                                  │                        │             │
│                                  │ Write Txns   Read State│             │
└──────────────────────────────────┼────────────────────────┼────────────┘
                                   │                        │
┌──────────────────────────────────▼────────────────────────▼────────────┐
│                      ALGORAND BLOCKCHAIN LAYER                          │
│                                                                          │
│  ┌───────────────────────┐  ┌───────────────────────┐                 │
│  │  GroupManager (App 1) │  │ ExpenseTracker (App 2)│                 │
│  ├───────────────────────┤  ├───────────────────────┤                 │
│  │ Global State:         │  │ Global State:         │                 │
│  │ - group_counter       │  │ - total_expenses      │                 │
│  │ - admin               │  │ - contract_version    │                 │
│  │                       │  │                       │                 │
│  │ Box Storage:          │  │ Box Storage:          │                 │
│  │ - group_{id}_name     │  │ - expense_{id}_data   │                 │
│  │ - group_{id}_members  │  │ - expense_{id}_splits │                 │
│  │ - group_{id}_admin    │  │                       │                 │
│  │                       │  │ Methods:              │                 │
│  │ Methods:              │  │ - add_expense()       │                 │
│  │ - create_group()      │  │ - get_expense()       │                 │
│  │ - add_member()        │  │ - calculate_balances()│                 │
│  │ - remove_member()     │  │                       │                 │
│  └───────────────────────┘  └───────────────────────┘                 │
│                                                                          │
│  ┌─────────────────────────────────────────────────┐                   │
│  │      SettlementExecutor (App 3)                 │                   │
│  ├─────────────────────────────────────────────────┤                   │
│  │ Methods:                                        │                   │
│  │ - initiate_settlement()                         │                   │
│  │ - execute_settlement() [Atomic Group]           │                   │
│  │   Group[0]: Payment(debtor → creditor)         │                   │
│  │   Group[1]: AppCall(verify & mark settled)     │                   │
│  └─────────────────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────────────────┘
                                   │
                                   │ Events & State
                                   │
┌──────────────────────────────────▼──────────────────────────────────────┐
│                          DATA LAYER                                     │
│  ┌──────────────────────────┐     ┌────────────────────────────────┐  │
│  │  SQLite / PostgreSQL     │     │  Algorand Indexer              │  │
│  ├──────────────────────────┤     ├────────────────────────────────┤  │
│  │ Tables:                  │     │ - Historical transactions      │  │
│  │ - users                  │     │ - Application calls            │  │
│  │ - groups                 │     │ - State changes                │  │
│  │ - group_members          │     │ - Event logs                   │  │
│  │ - expenses               │     └────────────────────────────────┘  │
│  │ - expense_splits         │                                          │
│  │ - settlements            │                                          │
│  │ - transactions (indexed) │                                          │
│  │ - balances (cache)       │                                          │
│  └──────────────────────────┘                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow: Create Expense

```
┌────────────┐
│   User     │
│ (Frontend) │
└──────┬─────┘
       │ 1. POST /api/v1/expenses
       │    {group_id, amount, description, splits}
       ▼
┌────────────────┐
│  Backend API   │ 2. Validate input
│  (FastAPI)     │ 3. Check user is group member
└──────┬─────────┘ 4. Build transaction
       │
       │ 5. Call ExpenseTracker.add_expense()
       ▼
┌─────────────────┐
│ Smart Contract  │ 6. Validate caller is member
│ (ExpenseTracker)│ 7. Store expense in box
└──────┬──────────┘ 8. Emit event
       │ 9. Return expense_id
       ▼
┌────────────────┐
│  Backend API   │ 10. Store in database
└──────┬─────────┘ 11. Update balance cache
       │ 12. Return response
       ▼
┌────────────┐
│   User     │ 13. Display confirmation
│ (Frontend) │
└────────────┘
       │
       │ 14. Indexer picks up transaction
       ▼
┌──────────────┐
│  Indexer     │ 15. Update transaction log
│  Service     │ 16. Emit WebSocket event
└──────┬───────┘
       │
       ▼
┌────────────┐
│   User     │ Real-time update shown
│ (Frontend) │
└────────────┘
```

---

## 🤝 Data Flow: Execute Settlement

```
User A (Debtor)                 Backend API                Smart Contract
      │                              │                            │
      │ 1. Get settlement plan       │                            │
      ├─────────────────────────────>│                            │
      │                              │ 2. Calculate net balances  │
      │                              │ 3. Optimize transactions   │
      │<─────────────────────────────┤                            │
      │   [A→B: 50 ALGO]             │                            │
      │   [A→C: 25 ALGO]             │                            │
      │                              │                            │
      │ 4. Approve settlement        │                            │
      ├─────────────────────────────>│                            │
      │                              │ 5. Build atomic group:     │
      │                              │    [0] Payment(A→B, 50)    │
      │                              │    [1] AppCall(execute)    │
      │<─────────────────────────────┤                            │
      │   [Unsigned transactions]    │                            │
      │                              │                            │
      │ 6. Sign with wallet          │                            │
      │                              │                            │
      │ 7. Submit signed group       │                            │
      ├─────────────────────────────>│────────────────────────────>│
      │                              │                            │ 8. Atomic execution:
      │                              │                            │    - Verify payment
      │                              │                            │    - Mark settled
      │                              │<────────────────────────────┤
      │                              │   [Transaction confirmed]   │
      │                              │                            │
      │<─────────────────────────────┤ 9. Update database         │
      │   [Settlement complete]      │ 10. Update balances        │
      │                              │ 11. Notify users           │
      │                              │                            │
      │ 12. Show confirmation        │                            │
```

---

## 📊 Database Entity Relationships

```
┌──────────┐
│  users   │
│──────────│
│ id       │◄────┐
│ wallet   │     │
│ nonce    │     │
└──────────┘     │
                 │ wallet_address
                 │
              ┌──┴──────────┐         ┌────────────┐
              │group_members│◄───────┤│   groups   │
              │─────────────│         │────────────│
              │ group_id    │         │ id         │◄────┐
              │ wallet_addr │         │ chain_id   │     │
              │ role        │         │ name       │     │
              └─────────────┘         │ admin_addr │     │
                                      └────────────┘     │
                                            │            │
                                            │            │
                                            │ group_id   │ group_id
                                            │            │
                                            │            │
                                      ┌─────▼──────┐     │
                                      │  expenses  │     │
                                      │────────────│     │
                                      │ id         │     │
                                      │ chain_id   │◄────┼──┐
                                      │ group_id   │     │  │
                                      │ amount     │     │  │
                                      │ payer_addr │     │  │ expense_id
                                      │ settled    │     │  │
                                      └────────┬───┘     │  │
                                               │         │  │
                                               │         │  │
                         ┌─────────────────────┼─────────┘  │
                         │                     │            │
                  expense_id              expense_id   expense_id
                         │                     │            │
                         │                     │            │
                  ┌──────▼────────┐     ┌──────▼────────┐  │
                  │expense_splits │     │  settlements  │◄─┘
                  │───────────────│     │───────────────│
                  │ expense_id    │     │ expense_id    │
                  │ wallet_addr   │     │ from_addr     │
                  │ amount        │     │ to_addr       │
                  │ settled       │     │ amount        │
                  └───────────────┘     │ txn_id        │
                                        │ status        │
                                        └───────────────┘

                  ┌───────────────┐     ┌───────────────┐
                  │  balances     │     │ transactions  │
                  │───────────────│     │───────────────│
                  │ group_id      │     │ txn_id        │
                  │ wallet_addr   │     │ block_number  │
                  │ balance       │     │ type          │
                  │ updated_at    │     │ sender        │
                  └───────────────┘     │ metadata      │
                                        └───────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│   Frontend   │
└──────┬───────┘
       │ 1. User clicks "Connect Wallet"
       │
       ▼
┌──────────────────────┐
│ POST /auth/challenge │
│ {wallet_address}     │
└──────┬───────────────┘
       │
       ▼
┌──────────────┐
│   Backend    │ 2. Generate random nonce
│              │ 3. Store nonce (5 min TTL)
│              │ 4. Create message:
│              │    "AlgoCampus Login\n
│              │     Address: {addr}\n
│              │     Nonce: {nonce}\n
│              │     Time: {timestamp}"
└──────┬───────┘
       │ 5. Return {nonce, message}
       ▼
┌──────────────┐
│   Frontend   │ 6. Request wallet signature
└──────┬───────┘    via AlgoSigner/WalletConnect
       │
       ▼
┌──────────────────┐
│  User's Wallet   │ 7. Sign message with private key
└──────┬───────────┘
       │ 8. Return signature
       ▼
┌──────────────┐
│   Frontend   │
└──────┬───────┘
       │ 9. POST /auth/verify
       │    {wallet_address, signature, nonce}
       ▼
┌──────────────┐
│   Backend    │ 10. Verify signature using Ed25519
│              │ 11. Check nonce validity
│              │ 12. Generate JWT access token
│              │ 13. Generate JWT refresh token
└──────┬───────┘
       │ 14. Return {access_token, refresh_token}
       ▼
┌──────────────┐
│   Frontend   │ 15. Store tokens
│              │ 16. Include in Authorization header
│              │     for all future requests:
│              │     "Bearer {access_token}"
└──────────────┘
```

---

## 📦 Contract Storage Strategy

### GroupManager - Box Storage Layout
```
┌─────────────────────────────────────────────────────────────┐
│ Box Key                 │ Value Type    │ Size              │
├─────────────────────────┼───────────────┼───────────────────┤
│ group_1_name            │ String        │ Variable (≤100)   │
│ group_1_desc            │ String        │ Variable (≤500)   │
│ group_1_admin           │ Address       │ 32 bytes          │
│ group_1_members         │ Address[]     │ 32n bytes         │
│ group_1_active          │ Bool          │ 1 byte            │
│ group_1_created         │ UInt64        │ 8 bytes           │
└─────────────────────────────────────────────────────────────┘
```

### ExpenseTracker - Box Storage Layout
```
┌─────────────────────────────────────────────────────────────┐
│ Box Key                 │ Value Type    │ Size              │
├─────────────────────────┼───────────────┼───────────────────┤
│ expense_1_group_id      │ UInt64        │ 8 bytes           │
│ expense_1_amount        │ UInt64        │ 8 bytes           │
│ expense_1_payer         │ Address       │ 32 bytes          │
│ expense_1_description   │ String        │ Variable (≤500)   │
│ expense_1_splits        │ Packed Bytes  │ Variable          │
│ expense_1_settled       │ Bool          │ 1 byte            │
└─────────────────────────────────────────────────────────────┘

Splits Encoding (packed bytes):
┌────────────┬────────────┬────────────┐
│ Address(32)│ Amount(8)  │ Address(32)│ ... (repeats)
└────────────┴────────────┴────────────┘
```

---

## 🎯 API to Contract Mapping

| API Endpoint | Contract Method | Purpose |
|-------------|----------------|---------|
| `POST /groups` | `GroupManager.create_group()` | Create new group |
| `POST /groups/{id}/members` | `GroupManager.add_member()` | Add member |
| `DELETE /groups/{id}/members/{addr}` | `GroupManager.remove_member()` | Remove member |
| `GET /groups/{id}` | `GroupManager.get_group_info()` | Get metadata |
| `POST /expenses` | `ExpenseTracker.add_expense()` | Record expense |
| `GET /groups/{id}/balances` | `ExpenseTracker.calculate_balances()` | Get balances |
| `POST /settlements/execute` | `SettlementExecutor.execute_settlement()` | Pay debt |

---

## 🚀 Quick Reference: Common Operations

### Start Development Environment
```bash
# Terminal 1: Backend
cd backend && venv\Scripts\activate && uvicorn app.main:app --reload

# Terminal 2: Frontend  
cd projects/frontend && pnpm dev

# Terminal 3: LocalNet
algokit localnet start
```

### Test Authentication
```bash
# 1. Get challenge
curl -X POST http://localhost:8000/api/v1/auth/challenge \
  -H "Content-Type: application/json" \
  -d '{"wallet_address":"YOUR_ADDRESS"}'

# 2. Verify signature (after signing)
curl -X POST http://localhost:8000/api/v1/auth/verify \
  -H "Content-Type: application/json" \
  -d '{"wallet_address":"YOUR_ADDRESS","signature":"SIG","nonce":"NONCE"}'
```

### Create Group
```bash
curl -X POST http://localhost:8000/api/v1/groups \
  -H "Authorization: Bearer YOUR_JWT" \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Group","description":"Test"}'
```

### Check Health
```bash
curl http://localhost:8000/health
```

---

**For detailed implementation, see ARCHITECTURE.md**
